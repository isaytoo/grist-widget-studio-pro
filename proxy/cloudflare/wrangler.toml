name = "grist-widget-studio-proxy"
main = "worker.js"
compatibility_date = "2024-09-01"

# Optional: secrets (set via `wrangler secret put PROXY_TOKEN`)
# [vars]
# ALLOWED_HOSTS = ""
# BLOCKED_HOSTS = ""

# Routes (optional custom domain)
# [[routes]]
# pattern = "proxy.yourdomain.com/*"
# zone_name = "yourdomain.com"
